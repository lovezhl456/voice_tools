# 仓库协作约定

- 版本规则与迭代台账统一维护在 [CHANGELOG.md](CHANGELOG.md)。开始功能开发前登记目标版本和分支；使用 `0.功能版本.修订版本`，大版本只由项目所有者决定，新功能递增中间位且末位从 `1` 开始，已有功能更新或修复递增末位。分支、PR、交付版本按台账保持一致；未完成项不得标为已交付。

- 所有改动（包括代码、文档和项目约定）必须在独立工作分支提交，推送该分支并创建以 `main` 为目标的 PR。禁止直接在 `main` 上修改或提交，禁止直接推送 `main`，也不得先在本地合并到 `main` 再推送来绕过 PR。创建 PR 不等于获得合并授权；合并按用户授权执行。
- 每轮开始修改前，先执行 `git fetch origin main`。若当前在 `main`，先从最新 `origin/main` 创建符合版本命名规则的工作分支；若已有工作分支，再通过 `git merge --no-edit origin/main` 合入最新主线。不要只检查分支状态或使用未经更新的本地 main。
- 同步前检查工作区，妥善保留未提交改动及其他工作树；不要用 reset 或覆盖文件的方式完成同步。处理合并冲突后，按实际影响验证。
- 每次提交 PR 前，对最终差异做深度检查：功能边界、失败路径、回归风险、文档与证据一致性。修复发现的问题，并重跑受影响的检查；未通过或未验证项如实说明，不用历史通过记录代替当前验收。

## PR 提交前的可读性核查

- 每次创建或更新含代码变更的 PR 前，必须使用 `code-readability` Skill 核查相对最新 `origin/main` 的最终差异及必要调用点。按当前会话的 Skill 清单定位并读取 `SKILL.md`；本机安装路径为 `~/.codex/skills/code-readability/SKILL.md`。
- 核查命名、控制流、函数职责、重复逻辑与抽象、注释和风格一致性。修复本次范围内的问题，保留行为、公共接口、数据格式和执行顺序；超出本次范围的建议单独记录，不扩展为全仓重构。
- 修正后执行与实际改动相称的验证；核查后若代码再次变化，应复查受影响部分。格式化通过不能代替行为验证，也不要求无关的全仓测试。
- PR 说明必须包含“可读性核查”，简述检查范围、结论、已处理或保留的问题及相关验证。没有明显问题时如实说明；纯文档 PR 标记“不适用：无代码变更”。Skill 不可用时记录原因，按上述检查项手工核查，并明确未执行 Skill。

## 测试选择与结果复用

- 在编写代码后，绝不编写单元测试。
- 强烈优先使用 E2E 测试作为唯一的测试机制。用它们验证复杂功能是否正常工作。在 E2E 测试结束时，生成一个可验证且可重复的产物。
- 如果必须孤立测试一个系统，首先写下它可能失败的所有方式，然后再编写代码。
- 隔离测试是有证据的例外：在编码前记录失败方式、用户影响、现有 E2E 为什么无法捕获，以及可复现输入；只测试这些缺口。不能用默认值、内部调用次数、实现镜像或静态字符串断言替代用户流程验证。已有隔离测试只有能捕获 E2E 遗漏的真实 bug 才保留；审计见 [测试精简记录](docs/test-pruning.md)。
- 删除已有测试前，逐条核对替代 E2E 的实际入口、输入、分支、产物和拒绝条件。调用内部函数、另一个 CLI 入口、不同后端或只验证正常路径，都不构成等价覆盖；同一测试混有重复断言和独有断言时，保留独有部分。证据不足时保留并写明缺口，不能以精简数量为目标。
- schema、版本、命令参数、默认路径、安装依赖及任务参数／输出分类可能是公共契约，不能仅因断言涉及常量就删除。用具体故障及用户影响判断价值；必要时做受控故障注入，分别确认原测试和声称替代它的 E2E 能否发现错误，记录实际执行范围。
- E2E 从实际 CLI／安装包／浏览器入口走完业务流程，核验保存、导出及重新打开的结果。产物至少包含输入或确定性生成方式、执行命令、代码／安装包标识、结果文件和校验信息；不能只有控制台通过数量。HTTP 假服务、模型替身及合成音频须明确标注，不能据此声称真实设备或业务准确率通过。

- 修改已有用户流程前，在计划或 PR 中列出受影响的原有能力，标明保留／增强／移除及验证入口。未明确要求移除的能力默认保留；有意移除须说明用户影响和授权来源。新增页面优先接入既有共享组件，不能只检查新加的功能。
- 对自动通过、依赖就绪、成功状态等关键结论，先列出必须拒绝的冲突、缺失与时序异常，并把反例加入仓库回归；缺陷修复不能只验证修复后的正常样例。
- 用户界面按完整流程验收，从最终入口走到保存／导出／重新打开，逐项对照旧能力。QA 复核以 [R01–R08 基线](docs/review-acceptance.md) 为准，执行安装 wheel 的 `python scripts/check_review.py`；脚本和夹具须留在仓库，不能仅保存到个人验收目录。
- 完成前逐项对照原始请求、后续修正、保留能力和拒绝条件。每项结论标明对应自动测试、实际界面、真实模型／设备或业务样本证据；明确未验证项，不用通过数量替代完整性判断。PR 使用仓库模板，必需检查失败或未执行时不宣称完整交付。

- 模块内部小改动先运行覆盖本模块及调用方的 E2E；仅对已记录的覆盖缺口运行保留的隔离测试，不默认全仓运行。公共接口、CLI 契约、共享数据格式或跨工具流程变化，先选测，再完成一次默认全量回归。
- 含代码或测试变更的 PR 正式交付前，对最终代码状态执行一次默认全量回归；可复用本轮已经对同一状态完成的全量结果，不因提交 Git、修改 PR 说明或重复审查再全跑。后续代码变化先重跑受影响测试，交付前确保最终状态有全量结果；仅说明文档变化不使代码验证失效。
- 默认全量包括安装 wheel 的复核与检测／编辑／工作区浏览器 E2E，以及精简后保留的 Python discovery、Node 测试和 Studio/CLI 兼容检查。使用临时命令环境关闭真实模型、latency 真实引擎与 SIP 原生回环开关。离线 tshark、tcpdump、FFmpeg 等普通集成测试仍在其中。现有补充测试不构成新增单元测试的理由。依赖、源码来源检查、命令和选测关系见 [测试指南](docs/testing.md)。
- 真实模型、ViSQOL 后端、latency 真实引擎和 SIP 原生回环独立运行；对应接入、原生后端或依赖变化时执行相关专项。NISQA 假评分器服务、latency 契约与渲染测试属于普通测试，不需权重或真实引擎。
- 纯说明文档检查内容、路径和链接；新增或修改可执行示例时验证对应命令。前端变化运行相关 Node、CLI 兼容检查，页面或交互变化另做桌面／移动端浏览器验收。
- 记录代码基线、选测理由、实际命令、通过／失败／跳过和环境限制。受改动影响的必要用例未执行时，不宣布该部分验收通过；不得新增跳过来掩盖失败。HOMER 本机端口权限受限时补跑受阻组，复用其余已通过结果。

## 计划自检

- 每次计划定稿或实质修订后，检查目标遗漏、功能影响、依赖、文档、验证和交付；发现问题先修订计划再复核，最多三轮，没有明显问题则提前结束。
- 记录每轮发现的问题及修订；第三轮仍有未解决问题时明确列出，不将计划标为完备。实施中发现影响范围变化时先更新计划，并按上述规则复核。
