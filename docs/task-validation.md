# 0.12.1 验证记录

日期：2026-09-20。代码基线 `fc453bd`，分支 `feat/v0.12.1-task-delivery`。本记录区分实际运行、离线测试与目标机待验证范围。

## 已完成

| 检查 | 实际结果 |
|---|---|
| Python 全量回归 | 377 项，346 通过、31 跳过，约 75 秒；跳过项需要可选模型/设备/外部条件 |
| 收尾任务专项 | 18 项在 macOS Python 3.9.6 和最终 Linux ARM64 镜像中分别通过；覆盖最后的执行端参数类型校验 |
| SIP Studio | 24 项 Node 测试通过；五类断言在 scenario / Studio 文档之间往返保留 |
| 参数发现 | 9 个业务工具、51 个操作；7 个工作台模板通过当前 CLI 任务合同校验 |
| 分发安装 | 构建 wheel，安装到独立目录后导出工作台；最终 wheel 离线装入 Docker，`pip check` 通过 |
| Docker 后台流程 | `vt pack → run → status/logs → collect → review` 实际通过；3 步、20 组合成录音、147 个结果文件验签通过 |
| Mac → Linux 迁移 | 在 Mac 临时目录创建带绝对播放路径的场景；打包后删除原目录，只挂入 ZIP 到 Linux 容器，SIP 离线校验完成 |
| 中断保留证据 | 实际停止正在执行 NISQA 的后台容器；当前步骤 interrupted、后续步骤 cancelled，17 个文件正常收集 |
| 包和输入边界 | 篡改/越界 ZIP、映射逃逸、重复输出、符号链接、缺失素材、模型/凭据拒收通过；ViSQOL CSV 和重命名 SQLite 依赖迁移通过 |

全量回归之后又收紧了执行端覆盖参数的类型校验，并修复无效 UI 重排时的选中状态；这部分用上述 18 项任务专项及浏览器交互复测，没有用旧测试代替受影响检查。模型真实运行另列如下，不以跳过的单元测试宣称模型通过。

## 统一运行环境与实际业务回环

原生依赖在 Linux ARM64 容器实际编译/安装：PJSIP 2.17 的 Python 绑定、SIPp、tshark、FFmpeg、NISQA CPU 依赖，以及固定 ViSQOL 提交 `38d0b0163e441047d4429bf07ad09e5b9031d02c`。ViSQOL 的 ARM64 构建约 9 分钟；这不是资源下限或容量基准。

本机最终镜像：`voice-tools-executor:0.12.1`，ARM64，约 2.02 GB。运行环境基底按仓库 Dockerfile 构建，收尾源码通过离线 wheel 覆层更新，未重新下载依赖。

- 最终镜像 ID：`sha256:1b18eee5f7ae758a7fc47a428777bf5c48cd2e7c80a65b9a28353f838ac00d35`。
- 最终 wheel SHA-256：`b5b5b4f5f9550d702c132d8eb29ca9000696f288444ec2617674a173b1b21c6b`。
- 原生依赖基底：`sha256:c8e843091ee8e6c08d94a382ee7f84c39585428158996032c643ae8f23667430`。
- 最终镜像的 18 项任务测试使用 `--network none`。

实际回执中保留每次运行时的 image ID；业务评分和中断回环使用同一原生依赖基底上较早的源码覆层，随后修改只涉及输入路径/数据库识别、执行端参数校验和界面，均重跑了受影响的任务专项。

| 实际任务 | 结果及适用边界 |
|---|---|
| NISQA 官方示例音频 | 容器内 1 文件、1 记录评分成功、0 错误；页面可定位原始录音 |
| NISQA 合成双轨 | 本机 4 个分段/声道记录中 3 个评分、1 个静音证据不足；整体保留 partial 状态，没有把空值变成分数 |
| ViSQOL 官方配对音频 | Linux ARM64 后端实际评分完成，speech MOS-LQO 为 3.3158771991729736；样本约 2.74 秒，短于通常建议范围的警告被保留 |
| 容器内 SIPp UAS → PJSUA2 | 外部网络关闭，只使用回环地址；200 应答断言通过，接收 RTP 包数为 0 的断言失败；任务记为 findings，保留两条独立断言证据 |
| 人工标签往返 | 实际浏览器导出 CSV 后，原生 `qa promote --dataset-kind synthetic` 接收 1 项人工测试标签；该标签只是验收材料，不作为真实业务黄金结论 |

音频样本沿用固定 ViSQOL 官方演示来源；模型仍须符合各自许可。分数证明后端与数据流程能运行，不证明真实线路听感准确度或故障根因。

## 浏览器验收

使用真实 Chromium 浏览器检查：默认桌面布局、1440×900 参数侧栏、390×844 窄屏；根导航及工作台/复查/Studio 页面和静态资源均可访问。

实际操作包括：模板选择、任务编辑与导出、有效/无效文件导入、无效步骤重排回滚、键盘选中、长文件名、多录音列表、无评分状态、无匹配筛选、录音波形、左右声道、片段定位、人工标签导出、评分定位原音频、Studio 五类断言配置和错误参数提示。窄屏没有整页横向溢出，页签条自身可横向滚动。

修复并复测了播放器切页后的空 DOM 回调、标签导出前最新输入未同步，以及 Studio 断言入口在窄屏被隐藏的问题。修复后的验收未出现新增控制台错误。

本机入口：`http://127.0.0.1:8080/voice-tasks/`，已接入总导航。页面中的示例均标明合成材料、官方样本或容器内回环。

## 尚未验证

- 未在用户实际执行机运行，也未连接其 SIP 线路、HOMER 服务或 SSH 抓包主机。
- 本机 Docker Engine 为 23.0.5，未证明新版 Docker Desktop host networking 的 Mac 对外 SIP 行为；容器内回环不能替代此验证。
- 本轮未在物理 Linux 主机验证网卡抓包或原始包回放；Linux CAP_NET_RAW 配置已进入镜像，但接口、防火墙和链路需目标机验收。
- 本轮实际构建和运行的是 ARM64 统一镜像。amd64 构建入口已提供，当前统一版本未再次完整构建/运行；不能把此前独立 ViSQOL/NISQA 镜像的结果算作本轮统一镜像验收。
- 未做真实录音准确率、生产负载、长期驻留及大规模容量测试。31 项条件跳过的回归没有被计为通过。
